[package]
name = "medullah-multipart"
version = "0.3.0"
edition = "2021"
license = "MIT"
description = "Library For Handling File Uploads Based on Ntex"
repository = "https://github.com/spiralover/medullah-multipart"

[dependencies]
futures = "0.3"
ntex-multipart = "2.0"
tokio = { version = "1.42", default-features = false, features = ["fs", "io-util"] }
ntex = { version = "2.10", default-features = false, features = ["tokio"] }

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
