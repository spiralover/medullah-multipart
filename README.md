# Medullah Multipart
This is a simple multipart parser built for [Ntex.rs](https://github.com/ntex-rs/ntex)

## Installation
Add this to your `Cargo.toml`:
```toml
[dependencies]
medullah-multipart = { version = "0.4.0" }
```
